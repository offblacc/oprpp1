package hr.fer.oprpp1.hw04.db;

public enum QueryTokenType {
    QUERYKW,
    JMBAG,
    LASTNAME,
    FIRSTNAME,
    AND,
    STRING,
    LIKE, // LIKE
    COMPARISON_OPERATOR, // > < >= <= = !=
    EOF
}
